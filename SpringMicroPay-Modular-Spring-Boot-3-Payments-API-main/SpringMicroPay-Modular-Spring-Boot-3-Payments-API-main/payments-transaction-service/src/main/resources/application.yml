server:
  port: 8082
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/txn-db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
